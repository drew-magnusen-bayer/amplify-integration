// Generated by CoffeeScript 1.10.0
(function() {
  var dependencies, getBinding;

  dependencies = {
    services: require('./services'),
    devMode: process.env.NODE_ENV !== 'production'
  };

  getBinding = function(name) {
    var devMode, services, value;
    services = dependencies.services, devMode = dependencies.devMode;
    value = services[name];
    if (!value && !devMode) {
      throw new Error("required service binding " + name + " not found");
    }
    return value;
  };

  getBinding.dependencies = dependencies;

  module.exports = getBinding;

}).call(this);
