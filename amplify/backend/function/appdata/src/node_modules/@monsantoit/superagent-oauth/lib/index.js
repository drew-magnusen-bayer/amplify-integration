const agent = require("./failoverAuthorizedAgent")
const _ = require("lodash")

module.exports = {
  agent,
  azureAgent: _.mapValues({ ...agent }, (action) => action.bind({ authProvider: "azure" })),
}
