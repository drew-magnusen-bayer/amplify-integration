const get = require('lodash/get')

module.exports = {
    /**
     *  Used with processor options, allowing processors to look at the config,
     *  or provide direct values
     */
    getOption(config, option) {
        if (option.inConfig) {
            return get(config, option.inConfig)
        }

        return option
    }
}
