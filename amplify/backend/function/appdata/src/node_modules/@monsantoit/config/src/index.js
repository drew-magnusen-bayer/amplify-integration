const Config = require('./config/index')
const processor = require('./processors/index')
const source = require('./sources/index')
const env = require('./env')

module.exports = {
    Config,
    processor,
    source,
    env
}
