const { azureClientSecret } = require("../.env.json")

if (!azureClientSecret) {
  throw new Error(`No client secret found in .env.json!`)
}

const { azureConfig } = require(`./${process.env.__DEV_TEAM}.json`)

module.exports = {
  azureConfig: { ...azureConfig, clientSecret: azureClientSecret },
}
